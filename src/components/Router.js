import {Route, HashRouter as Router, Routes} from "react-router-dom";


export const AppRouter = () => {
    return (
        <Router>
            <Routes>
                <Route/>
            </Routes>
        </Router>
    );
};